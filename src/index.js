

/* import productos from "./productManager.js"

let persistirProducto = async () => {
    try {
        let productosTotales = await productos.getProducts()

        if (productosTotales && Array.isArray(productosTotales)) {
            console.log(`Productos encontrados en ProductManager: ${productosTotales.length}`)
        } else {
            console.log("No se pudieron obtener los productos o la respuesta no es un array.")
        }
    } catch (error) {
        console.error("Error al obtener productos:", error)
    }
}

persistirProducto() */

